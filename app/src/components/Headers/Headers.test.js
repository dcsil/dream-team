import Header from "./Header";
import UserHeader from "./UserHeader";

import {commonRenderTest} from "CommonRenderTest"

commonRenderTest.bind(this)(Header)

commonRenderTest.bind(this)(UserHeader)

